# Blanktopia's Moromoro config

## Usage

1. Install Jsonnet:
```sh
brew install jsonnet
```
OR
```sh
pip install jsonnet
```

2. Generate files using Jsonnet by running this in the root directory:
```sh
./jsonnet.sh
```
